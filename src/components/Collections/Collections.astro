---
import CollectionCard from "./CollectionCard.astro";
import type { TCollectionCard } from "./CollectionCard.astro";
import collections from "public/maps/collections.json";
---

<section class="my-16 mx-auto">
  <h3 class="text-center">Mis colecciónes</h3>

  <div
    class="collectionCardList grid grid-cols-[repeat(auto-fit,minmax(250px,1fr))] place-items-center justify-center gap-x-4 gap-y-8 mx-auto my-8 max-w-5xl"
  >
    {
      (collections as TCollectionCard[]).map(collection => (
        <CollectionCard {...collection} />
      ))
    }
  </div>
</section>
